name: building_material_billing
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  path: ^1.9.1
  pdf: ^3.13.0
  printing: ^5.15.0
  share_plus: ^13.3.0
  esc_pos_utils_plus: ^2.0.4
  shared_preferences: ^2.5.5
  sqflite: ^2.4.3
  url_launcher: ^6.3.2


  print_bluetooth_thermal:
    path: print_bluetooth_thermal_patched
  unified_esc_pos_printer:
    path: unified_esc_pos_printer_patched

  firebase_core: ^4.12.0
  cloud_firestore: ^6.7.0
  firebase_auth: ^6.5.5
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/NotoSans-Regular.ttf